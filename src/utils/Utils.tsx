import { Dimensions } from 'react-native'

const { width, height } = Dimensions.get('window')
const DOT_SIZE = 30
const LOGO_HEIGHT = 70
const LOGO_WIDTH = 220
const BACKGROUND_SIZE = width * 0.6
const DROPDOWN_HEIGHT = 30

export {
  width,
  height,
  LOGO_HEIGHT,
  LOGO_WIDTH,
  DOT_SIZE,
  DROPDOWN_HEIGHT,
  BACKGROUND_SIZE
}
