import { Dimensions } from 'react-native'

const { width, height } = Dimensions.get('window')
const DOT_SIZE = 40
const LOGO_HEIGHT = 70
const LOGO_WIDTH = 220

export { width, height, LOGO_HEIGHT, LOGO_WIDTH, DOT_SIZE }
